<UserControl x:Class="HouseControls.ControlListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:l="clr-namespace:HouseControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.Resources>
      <l:ThumbnailGenerator x:Key="ThumbnailGenerator" />
      <l:ShowHideColumnWidth x:Key="ShowHideColumnWidth" />

      <DataTemplate x:Key="TileTemplate">
        <StackPanel Height="100" Width="90">
          <Grid Width="70" Height="70" HorizontalAlignment="Center">
            <Image Source="{Binding Image, Converter={StaticResource ThumbnailGenerator}}" Margin="6,6,6,9"/>
          </Grid>
          <TextBlock Text="{Binding Name}" FontSize="13" 
                     HorizontalAlignment="Center" Margin="0,0,0,1" />
          <TextBlock Text="{Binding Type}" FontSize="9" 
                     HorizontalAlignment="Center" Margin="0,0,0,1" />
        </StackPanel>
      </DataTemplate>

      <l:PlainView x:Key="TileView"
                 ItemTemplate="{StaticResource TileTemplate}" />

      <DataTemplate x:Key="IconTemplate">
        <DockPanel>
          <Image DockPanel.Dock="Top" 
               Height="30" 
               Width="30" 
               HorizontalAlignment="Center" 
               Stretch="Fill" 
               Source="{Binding Image, Converter={StaticResource ThumbnailGenerator}}" 
               Margin="2"/>
          <TextBlock DockPanel.Dock="Bottom" 
                   HorizontalAlignment="Center"
                   Text="{Binding Name}" 
                   FontSize="13" />
        </DockPanel>
      </DataTemplate>

      <l:PlainView x:Key="IconView" 
                 ItemTemplate="{StaticResource IconTemplate}" />

      <DataTemplate x:Key="Checkbox">
        <CheckBox IsChecked="{Binding IsChecked}">
        </CheckBox>
      </DataTemplate>

      <DataTemplate x:Key="DisplayImage">
        <Grid Height="30">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <Image Grid.Column="0" Source="{Binding Image, Converter={StaticResource ThumbnailGenerator}}" Stretch="UniformToFill"/>
          <TextBlock Grid.Column="1" Text="{Binding Title}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        </Grid>
      </DataTemplate>

      <GridView x:Key="GridView">
        <GridViewColumn CellTemplate="{StaticResource Checkbox}"/>
        <GridViewColumn CellTemplate="{StaticResource DisplayImage}"/>
      </GridView>

    </Grid.Resources>
    <ListView Name="lv"
              FontSize="12">
    </ListView>
  </Grid>
</UserControl>
